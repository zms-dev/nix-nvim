{
    plugins.telescope = {
        enable = true;
        enabledExtensions = [ "notify" ];
        extensions = {
            undo = {
                enable = true;
                settings = {};
            };
            fzf-native = {
                enable = true;
                settings = {};
            };
            file-browser = {
                enable = true;
                settings = {};
            };
        };
    };
}